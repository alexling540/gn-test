import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#500000",
    },
    secondary: {
      main: "#998542",
    },
    success: {
      main: "#3ed488",
    },
    error: {
      main: "#E4002B",
    },
  },
});

export default theme;
